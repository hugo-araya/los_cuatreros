# Entero cap_invertir
# Real ganancia
cap_invertir = int(input('Ingrese capital: '))
ganancia = cap_invertir * 2 / 100
salida = 'Para una inversion de $'+str(cap_invertir)+' la ganancia es $'+str(ganancia)
print(salida)
